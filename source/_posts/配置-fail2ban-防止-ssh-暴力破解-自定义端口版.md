---
title: 配置 Fail2Ban 防止 SSH 暴力破解 (自定义端口版)
date: 2026-01-04T15:04:00.000+08:00
---


### \[教程] Debian 11 VPS 基础安全：配置 Fail2Ban 防止 SSH 暴力破解 (自定义端口版)

VPS 裸奔在公网上，每天都会有无数个脚本尝试暴力破解你的 SSH 密码。如果你已经修改了 SSH 默认端口（比如改为 56789），虽然能躲过大部分扫描，但加上 Fail2Ban 才是双重保险。

Fail2Ban 的原理很简单：监控日志 -> 发现有人多次输错密码 -> 自动把他的 IP 关进小黑屋。

#### 环境说明

* 系统：Debian 11
* SSH 端口：56789 (根据实际情况修改)

### 第一步：更新源并安装 Fail2Ban

首先更新系统软件包列表，然后直接安装：

### 第二步：配置防护规则 (关键)

Fail2Ban 默认只监控 22 端口，因为我们改了端口（56789），所以必须新建一个配置文件来告诉它监控哪里。

直接复制下面整段代码，粘贴到 SSH 终端并回车：

> 注意：如果你的 SSH 端口不是 56789，请修改 `port = 56789` 这一行。

### 第三步：启动并设置开机自启

配置写好后，重启服务让它生效：

### 第四步：验证是否生效

你可以使用以下命令查看 SSH 防护模块的状态：

如果看到 Status 显示为 `active`，说明防护罩已经打开了。
如果 `Banned IP list` 后面有 IP 地址，说明已经有“倒霉蛋”被封禁了。

### 附：Fail2Ban 常用维护命令

1. 查看被封禁的 IP 列表：

2. 误封了解封某个 IP (比如把你自己的解封)：

3. 查看 Fail2Ban 运行日志 (排错用)：

博主总结：
这三步操作虽然简单，但能挡住 99% 的 SSH 扫爆脚本。建议新开 VPS 后第一时间部署。
